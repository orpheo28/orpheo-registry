provider_id: aws-transcribe
service_name: "Amazon Transcribe"
entity: aws
entity_name: "Amazon Web Services"
layer: transcription
models: []

baa_available:
  value: true
  verified_at: 2026-08-13
  source_url: https://aws.amazon.com/compliance/hipaa-eligible-services-reference/
  confidence: medium
  note: >
    Listed as « AWS Transcribe [Includes Healthscribe] » — an INCLUSIVE
    annotation, where Bedrock carries a model exclusion on the same page. The two
    read in the same place and say opposite things: this list rewards reading
    line by line rather than skimming.
    THE BAA IS A PREREQUISITE, not a consequence of eligibility: « you agree not
    to use these HIPAA Eligible Services ... without first entering into an AWS
    business associate agreement ».
    Eligibility remains subject to the shared responsibility model: « Customers
    still must configure these services consistent with HIPAA requirements. »
    Compliance reference published by AWS, not the contract: medium confidence.

# no_training_commitment — NOT RECORDED
#   The AWS Service Terms contain clauses on AI service content usage, not read.
#   Do not infer from HIPAA eligibility.

# zero_retention_option — NOT RECORDED
#   No first-party source read.

# default_retention — NOT RECORDED
#   No first-party source read.

# data_residency — NOT RECORDED
#   AWS regions are documented elsewhere; no first-party page read establishes
#   residency for transcription data.

# dpa_eu — NOT RECORDED
#   No first-party source read.
